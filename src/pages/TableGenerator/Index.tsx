import TableGenerator from './TableGenerator';

const TableGeneratorIndex = () => {
  return (
    <div className="table-generator-container">
      <h1>Table Generator Challenge</h1>
      <p>Build a component that generates a table of numbers given the rows and columns.</p>
      <TableGenerator />
    </div>
  );
};

export default TableGeneratorIndex; 